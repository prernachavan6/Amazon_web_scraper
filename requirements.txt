# ─────────────────────────────────────────────
# requirements.txt — Amazon Scraper Flask App
# ─────────────────────────────────────────────

# Web framework
Flask==3.1.0

# Production WSGI server (replaces Flask dev server)
gunicorn==22.0.0

# HTTP scraping
requests==2.32.3

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.2.2          # faster HTML parser for BeautifulSoup

# Environment variable management
python-dotenv==1.0.1

# Date/time utilities (used internally by Flask)
Werkzeug==3.1.3